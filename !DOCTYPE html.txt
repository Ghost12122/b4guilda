<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>B4 @esportes</title>
<style>
  body {
    font-family: Arial, sans-serif;
    background-color: #0d0d0d;
    color: #ffffff;
    margin: 0;
    padding: 0;
    scroll-behavior: smooth;
  }

  header {
    background-color: #1a1a1a;
    padding: 20px;
    text-align: center;
  }

  header h1 {
    margin: 0;
    font-size: 2.5em;
    color: #00ffcc;
  }

  nav {
    background-color: #262626;
    padding: 10px;
    text-align: center;
  }

  nav a {
    color: #ffffff;
    margin: 0 15px;
    text-decoration: none;
    font-weight: bold;
  }

  nav a:hover {
    color: #00ffcc;
  }

  main {
    padding: 30px;
  }

  footer {
    background-color: #1a1a1a;
    text-align: center;
    padding: 10px;
    position: relative;
    bottom: 0;
    width: 100%;
  }

  .section {
    margin-bottom: 40px;
  }

  .section h2 {
    color: #00ffcc;
  }

  /* Estilo para lista de membros */
  .membro {
    cursor: default;
    padding: 8px;
    border-radius: 5px;
    transition: background 0.3s;
  }

  .membro:hover {
    background-color: #1a1a1a;
  }

  /* Estilo contador */
  #contador {
    font-size: 1.2em;
    font-weight: bold;
    color: #ff4444;
  }

  /* Botão música */
  #btnMusica {
    position: fixed;
    bottom: 20px;
    right: 20px;
    background-color: #00ffcc;
    border: none;
    padding: 12px 18px;
    border-radius: 50px;
    font-size: 16px;
    cursor: pointer;
    box-shadow: 0 0 10px #00ffcc;
    transition: background-color 0.3s ease;
    z-index: 1000;
  }

  #btnMusica:hover {
    background-color: #00cca3;
  }
</style>
</head>
<body>

<header>
  <h1>B4 @esportes</h1>
</header>

<nav>
  <a href="#sobre">Sobre</a>
  <a href="#membros">Líderes</a>
  <a href="#eventos">Eventos</a>
  <a href="#contato">Contato</a>
</nav>

<main>
  <section id="sobre" class="section">
    <h2>Sobre a B4</h2>
    <p>Somos a B4 @esportes, uma família unida que cresce junto e busca sempre o melhor.</p>
  </section>

  <section id="membros" class="section">
    <h2>Líderes</h2>
    <ul>
      <li class="membro">@Hancock B4</li>
      <li class="membro">@Caioz B4</li>
      <li class="membro">@Sares B4</li>
    </ul>
  </section>

  <section id="eventos" class="section">
    <h2>Próximos Eventos</h2>
    <ul>
      <li>⚔️ Guerras de guildas - Todos os sábados</li>
    </ul>
    <p>⏳ Tempo restante para a próxima Guerra: <span id="contador"></span></p>
  </section>

  <section id="contato" class="section">
    <h2>Contato</h2>
    <p>Quer se juntar à nossa guilda ou fazer uma parceria? Envie uma mensagem no WhatsApp:</p>
    <ul>
      <li><a href="https://wa.me/5531995626200" target="_blank" style="color:#00ffcc; text-decoration:none;">(31) 99562-6200</a></li>
      <li><a href="https://wa.me/553599818884" target="_blank" style="color:#00ffcc; text-decoration:none;">(35) 99818-884</a></li>
    </ul>
  </section>
</main>

<footer>
  <p>&copy; 2025 B4 @esportes. Todos os direitos reservados.</p>
</footer>

<!-- Botão para controlar música -->
<button id="btnMusica">🎵 Tocar Música</button>

<!-- Música de fundo -->
<audio id="musica" src="https://drive.google.com/uc?export=download&id=1fuKrZmlkB-Rqbcv0BwYLpxugk1ibORDC" loop></audio>

<script>
  // Função para calcular o tempo até o próximo sábado 00:00
  function tempoParaProximoSabado() {
    const agora = new Date();
    const diaSemana = agora.getDay(); // 0=Domingo, 6=Sábado
    let diasParaSabado = 6 - diaSemana;
    if (diasParaSabado < 0) {
      diasParaSabado += 7;
    }

    // Ajusta para sábado 00:00:00
    const proximoSabado = new Date(
      agora.getFullYear(),
      agora.getMonth(),
      agora.getDate() + diasParaSabado,
      0, 0, 0, 0
    );

    const diff = proximoSabado.getTime() - agora.getTime();
    return diff > 0 ? diff : 0;
  }

  function atualizarContador() {
    let diff = tempoParaProximoSabado();

    if (diff <= 0) {
      document.getElementById("contador").innerText = "A Guerra de guildas está acontecendo agora!";
      return;
    }

    const dias = Math.floor(diff / (1000 * 60 * 60 * 24));
    const horas = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutos = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
    const segundos = Math.floor((diff % (1000 * 60)) / 1000);

    document.getElementById("contador").innerText =
      `${dias}d ${horas}h ${minutos}m ${segundos}s`;
  }

  setInterval(atualizarContador, 1000);
  atualizarContador();

  // Controle do botão de música
  const btnMusica = document.getElementById("btnMusica");
  const musica = document.getElementById("musica");
  let tocando = false;

  btnMusica.addEventListener("click", () => {
    if (!tocando) {
      musica.play();
      btnMusica.textContent = "🔇 Parar Música";
      tocando = true;
    } else {
      musica.pause();
      btnMusica.textContent = "🎵 Tocar Música";
      tocando = false;
    }
  });
</script>
</body>
</html>